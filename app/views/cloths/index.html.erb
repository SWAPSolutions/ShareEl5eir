<p id="notice"><%= notice %></p>

<h1>Listing Cloths</h1>

<table>
  <thead>
    <tr>
      <th>Size</th>
      <th>Color</th>
      <th>User</th>
      <th>Quantity</th>
      <th>Description</th>
      <th>Donated</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cloths.each do |cloth| %>
      <tr>
        <td><%= cloth.size %></td>
        <td><%= cloth.color %></td>
        <td><%= cloth.user_id %></td>
        <td><%= cloth.quantity %></td>
        <td><%= cloth.description %></td>
        <td><%= cloth.donated %></td>
        <td><%= link_to 'Show', [@member , cloth] %></td>
        <td><%= link_to 'Edit', edit_member_cloth_path(@member , cloth) %></td>
        <td><%= link_to 'Destroy', [@member , cloth], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cloth', new_member_cloth_path %>
